postgres:
  host: localhost
  port: 5432
  user: postgres
  password: heslo123
  database: postgres

logging:
  level: info

webserver:
  enabled: true
  port: 8080
  mode: debug # release, test

keys:
  cacerts: "ca.crt"
  clientcert: "client.pem"
  clientkey: "client.key"

services:
  HealthcheckService: true