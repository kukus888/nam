postgres:
  dsn: "postgres://postgres:heslo123@localhost:5432/postgres?application_name=nam"

logging:
  level: info

webserver:
  enabled: true
  port: 8080
  mode: debug # release, test
  tls:
    enabled: false
    certpath: "cert.pem"
    keypath: "key.pem"

keys:
  cacerts: "cacert.pem"
  clientcert: ""
  clientkey: ""

services:
  HealthcheckService: true