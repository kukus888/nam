postgres:
  host: localhost
  port: 5432
  user: postgres
  password: heslo123
  database: postgres

logging:
  level: debug

webserver:
  enabled: true
  port: 8080
  mode: debug # or development

services:
  HealthcheckService: true